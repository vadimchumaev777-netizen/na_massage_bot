const TelegramBot = require("node-telegram-bot-api")
const express = require("express")

// Express сервер для Render
const app = express()
const port = process.env.PORT || 10000

app.get("/", (req, res) => {
  res.json({
    status: "OK",
    message: "🤖 Бот массажистки Алёны работает!",
    timestamp: new Date().toISOString(),
    uptime: process.uptime(),
  })
})

app.listen(port, () => {
  console.log(`🌐 HTTP сервер запущен на порту ${port}`)
})

// Telegram бот
const token = process.env.TOKEN
const ADMIN_CHAT_ID = 487996672 // ВАШ CHAT ID БЕЗ КАВЫЧЕК!

if (!token) {
  console.error("❌ Токен не найден!")
  process.exit(1)
}

const bot = new TelegramBot(token, { polling: true })

// Главное меню
const mainMenu = {
  reply_markup: {
    keyboard: [
      ["📅 Записаться на массаж", "💰 Прайс-лист"],
      ["👩‍⚕️ О специалисте", "❓ Частые вопросы"],
      ["📞 Контакты", "📍 Адрес и время работы"],
    ],
    resize_keyboard: true,
    one_time_keyboard: false,
  },
}

// Функция уведомления админа (ИСПРАВЛЕННАЯ)
async function notifyAdmin(userInfo, message) {
  if (!ADMIN_CHAT_ID) {
    console.log("⚠️ Chat ID не настроен")
    return
  }

  try {
    // Создаем ссылку на пользователя
    let userLink = ""
    if (userInfo.username) {
      userLink = `@${userInfo.username}`
    } else {
      userLink = userInfo.first_name || "Пользователь"
    }

    const notification = `🔔 НОВОЕ СООБЩЕНИЕ

👤 От: ${userLink}
📝 Имя: ${userInfo.first_name || "Неизвестно"} ${userInfo.last_name || ""}
🆔 ID: ${userInfo.id}
💬 Сообщение: "${message}"
📅 Время: ${new Date().toLocaleString("ru-RU")}`

    await bot.sendMessage(ADMIN_CHAT_ID, notification)
    console.log("✅ Уведомление отправлено админу")
  } catch (error) {
    console.log("❌ Ошибка уведомления:", error.message)
  }
}

// Команда /start
bot.onText(/\/start/, async (msg) => {
  const chatId = msg.chat.id
  const user = msg.from
  const firstName = user.first_name || "Друг"

  console.log(`👤 Пользователь ${firstName} (ID: ${user.id}) запустил бота`)

  // Уведомляем админа о новом пользователе
  notifyAdmin(user, "🚀 ЗАПУСТИЛ БОТА").catch(console.error)

  const welcomeMessage = `🌟 *Добро пожаловать, ${firstName}!*

Меня зовут *Алёна*, и я специалист по детскому массажу.

👩‍⚕️ *Моя квалификация:*
• Дипломированная медицинская сестра
• Профессиональная переподготовка по детскому массажу
• Более 5 лет практики работы с детьми

🎯 *Я помогаю при:*
✔️ Нарушениях мышечного тонуса
✔️ Кривошее
✔️ Коликах и запорах
✔️ Сколиозе и нарушении осанки
✔️ Проблемах со стопами

💝 *Мой подход:*
• Индивидуальный подход к каждому ребенку
• Комфортная атмосфера
• Работа на основе медицинских знаний

🎯 *Моя цель* — помочь вашему малышу расти здоровым!

Выберите нужный пункт в меню ⬇️`

  try {
    await bot.sendMessage(chatId, welcomeMessage, {
      parse_mode: "Markdown",
      ...mainMenu,
    })
    console.log("✅ Приветствие отправлено")
  } catch (error) {
    console.log("❌ Ошибка приветствия:", error.message)
  }
})

// Обработка всех сообщений
bot.on("message", async (msg) => {
  const chatId = msg.chat.id
  const text = msg.text
  const user = msg.from

  // Пропускаем команды
  if (text && text.startsWith("/")) {
    return
  }

  if (!text) {
    return
  }

  console.log(`📨 Сообщение от ${user.first_name} (ID: ${user.id}): "${text}"`)

  // ВСЕГДА уведомляем админа о любом сообщении
  notifyAdmin(user, text).catch(console.error)

  try {
    if (text === "📅 Записаться на массаж") {
      console.log("🎯 Записаться на массаж")

      await bot.sendMessage(
        chatId,
        `📅 *ЗАПИСЬ НА ДЕТСКИЙ МАССАЖ*

💬 *Для записи просто напишите в этот чат:*
• Имя и возраст ребенка
• Какой массаж нужен
• Удобное время
• Ваш телефон

📝 *Например:*
"Здравствуйте! Хочу записать сына Максима, 5 лет, на общеукрепляющий массаж. Удобно в среду после 15:00. Мой телефон: +7-999-123-45-67"

⚡ *Я получу ваше сообщение и сразу свяжусь с вами!*

📱 *Телефон для звонков:* +7 (914) 993-57-73
💬 *WhatsApp:* +7 (914) 993-57-73

Жду вашего сообщения! 💌`,
        {
          parse_mode: "Markdown",
        },
      )
    } else if (text === "📞 Контакты") {
      console.log("🎯 Контакты")

      await bot.sendMessage(
        chatId,
        `📞 *КОНТАКТЫ*

👩‍⚕️ *Специалист:* Алёна
🏥 *Детский массаж*

📱 *Телефон:* +7 (914) 993-57-73
📧 *Email:* alena.massage@mail.ru
💬 *WhatsApp:* +7 (914) 993-57-73

⏰ *Время работы:*
Понедельник - Пятница: 9:00 - 18:00
Суббота: 10:00 - 16:00
Воскресенье: выходной

📍 *Прием:* На дому у клиента или в кабинете
🚗 *Выезд:* По г. Чехов и ближайшему Подмосковью

⚡ *Быстрая связь:*
Отвечаю в течение 1-2 часов

💌 *Буду рада помочь вашему малышу!*`,
        {
          parse_mode: "Markdown",
        },
      )
    } else if (text === "💰 Прайс-лист") {
      console.log("🎯 Прайс-лист")

      await bot.sendMessage(
        chatId,
        `💰 *ПРАЙС-ЛИСТ ДЕТСКИЙ МАССАЖ*

👶 *Общеукрепляющий массаж (0-13 лет)*
Стоимость: 950₽ за сеанс

💪 *Массаж при мышечной дистонии*
Стоимость: 950₽ за сеанс

🏥 *Массаж при сколиозе (4-7 лет)*
Стоимость: 700₽ за сеанс

🤱 *Массаж при коликах/запорах*
• До 1 года: 500₽ + обучение мам
• От 1 до 4 лет: 750₽

🦴 *Массаж кривошеи*
• До 1 года: 650₽
• От 1 до 4 лет: 750₽
• От 4 до 13 лет: 800₽

🦶 *Массаж стоп (4-13 лет)*
Стоимость: 950₽ за сеанс

📋 *Рекомендуемый курс:* 10-15 сеансов

📱 *Записаться:* +7 (914) 993-57-73`,
        {
          parse_mode: "Markdown",
          ...mainMenu,
        },
      )
    } else if (text === "👩‍⚕️ О специалисте") {
      console.log("🎯 О специалисте")

      await bot.sendMessage(
        chatId,
        `👩‍⚕️ *АЛЁНА - СПЕЦИАЛИСТ ПО ДЕТСКОМУ МАССАЖУ*

🎓 *Образование и квалификация:*
• Дипломированная медицинская сестра
• Профессиональная переподготовка по детскому массажу
• Более 5 лет успешной практики работы с детьми

🏥 *Специализация:*
✔️ Нарушения мышечного тонуса
✔️ Кривошея
✔️ Пупочная грыжа
✔️ Колики и запоры у младенцев
✔️ Сколиоз и нарушения осанки
✔️ Проблемы стоп
✔️ Общеукрепляющий и развивающий массаж

💝 *Мой подход:*
• Работа основана на медицинских знаниях
• Индивидуальный подход к каждому ребенку
• Комфортная атмосфера для малышей
• Обучение родителей элементам массажа

🎯 *Результаты работы:*
• Улучшение мышечного тонуса
• Нормализация развития
• Укрепление здоровья малышей
• Довольные родители и здоровые дети

💌 *Моя миссия* — помочь каждому ребенку расти здоровым и крепким!`,
        {
          parse_mode: "Markdown",
          ...mainMenu,
        },
      )
    } else if (text === "❓ Частые вопросы") {
      console.log("🎯 Частые вопросы")

      await bot.sendMessage(
        chatId,
        `❓ *ЧАСТЫЕ ВОПРОСЫ*

*Q: Массаж поможет выровнять спину у ребенка 7 лет?*
A: Да, массаж — важная часть лечения. Он укрепит ослабленные мышцы и расслабит спазмированные, что способствует выравниванию. Но необходим комплекс: массаж + ЛФК + контроль ортопеда.

*Q: Делаете ли срочный массаж шеи при падении зрения?*
A: Нет. Такая ситуация требует срочной консультации невролога и офтальмолога. Массаж я могу делать только по их назначению после постановки диагноза.

*Q: Берете ли на массаж подростков 14 лет с больной спиной?*
A: Да, конечно. Перед курсом рекомендую получить консультацию ортопеда или невролога для исключения серьезных патологий.

*Q: С какого возраста можно делать массаж ребенку?*
A: Профилактический массаж — с 1 месяца. Лечебный — строго по показаниям врача с первых недель жизни.

*Q: Какой курс нужен и как часто?*
A: Стандартный курс — 10-15 сеансов, проводимых ежедневно или через день. Для стойкого результата курсы рекомендуется повторять 2-4 раза в год.

*Q: Что эффективнее: массаж или ЛФК?*
A: Это не взаимозаменяемые, а дополняющие процедуры. Массаж пассивно готовит мышцы. ЛФК активно укрепляет их. Максимальный эффект достигается их комбинацией.`,
        {
          parse_mode: "Markdown",
          ...mainMenu,
        },
      )
    } else if (text === "📍 Адрес и время работы") {
      console.log("🎯 Адрес и время работы")

      await bot.sendMessage(
        chatId,
        `📍 *АДРЕС И ВРЕМЯ РАБОТЫ*

🏠 *Форматы работы:*
• Выезд на дом к клиенту (предпочтительно)
• Прием в оборудованном кабинете

📍 *География работы:*
• г. Чехов (все районы)
• Ближайшее Подмосковье

⏰ *Расписание:*
*Понедельник - Пятница:* 9:00 - 18:00
*Суббота:* 10:00 - 16:00  
*Воскресенье:* выходной

🚗 *Выезд на дом:*
• Удобно для малышей
• Привычная обстановка
• Стерильные инструменты
• Все необходимое оборудование

🏥 *Кабинет:*
• Специально оборудованное место
• Комфортная температура
• Детская атмосфера
• Игрушки для отвлечения

📞 *Запись:* +7 (914) 993-57-73
💬 *WhatsApp:* +7 (914) 993-57-73

*Выберу оптимальное время для вас и вашего малыша!* 💝`,
        {
          parse_mode: "Markdown",
          ...mainMenu,
        },
      )
    } else {
      // Любое другое сообщение - это потенциальная заявка!
      console.log("🎯 Заявка на запись!")

      await bot.sendMessage(
        chatId,
        `💌 *Спасибо за заявку!*

Ваше сообщение: "${text}"

✅ *Я получила ваш запрос и свяжусь с вами в ближайшее время!*

📱 *Для срочной связи:*
• Телефон: +7 (914) 993-57-73
• WhatsApp: +7 (914) 993-57-73
• Email: alena.massage@mail.ru

⏰ *Отвечу в течение 1-2 часов!*

Буду рада помочь вашему малышу! 🌟`,
        {
          parse_mode: "Markdown",
          ...mainMenu,
        },
      )
    }

    console.log("✅ Ответ отправлен")
  } catch (error) {
    console.log("❌ Ошибка:", error.message)

    try {
      await bot.sendMessage(chatId, "Извините, произошла ошибка. Свяжитесь напрямую: +7 (914) 993-57-73")
    } catch (e) {
      console.log("❌ Критическая ошибка:", e.message)
    }
  }
})

// Обработка ошибок
bot.on("polling_error", (error) => {
  console.log("❌ Polling error:", error.message)
})

// Функция самопинга для предотвращения засыпания
function keepAlive() {
  const url = `https://na-massage-bot.onrender.com`

  setInterval(
    async () => {
      try {
        const response = await fetch(url)
        console.log(`🏓 Пинг: ${response.status} - ${new Date().toLocaleTimeString()}`)
      } catch (error) {
        console.log("❌ Ошибка пинга:", error.message)
      }
    },
    14 * 60 * 1000,
  ) // Каждые 14 минут
}

// Запускаем самопинг через 2 минуты после старта
setTimeout(keepAlive, 2 * 60 * 1000)

console.log("🤖 Бот массажистки Алёны запущен!")
console.log("📱 Telegram polling активен")
console.log(`📬 Уведомления будут отправляться на Chat ID: ${ADMIN_CHAT_ID}`)
console.log("🏓 Самопинг активирован для предотвращения засыпания")
